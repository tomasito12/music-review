# Deprecated: use deploy/production.crontab + scripts/install_production_cron.sh
#
# Production cron is installed automatically on each GitHub Deploy workflow run.
# Manual install on the server:
#   ./scripts/install_production_cron.sh
# From your laptop:
#   ./scripts/server.sh install-cron
#
# See deploy/production.crontab for the managed schedule (Infrastructure as Code).
