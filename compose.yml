services:
  caddy:
    image: caddy:2-alpine
    container_name: music-review-caddy
    restart: unless-stopped
    depends_on:
      - music-review
      - frontend
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./deploy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config

  music-review:
    build:
      context: .
    container_name: music-review
    restart: unless-stopped
    env_file:
      - .env
    environment:
      MUSIC_REVIEW_PROJECT_ROOT: /app
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./data:/app/data

  frontend:
    build:
      context: ./frontend
    container_name: music-review-frontend
    restart: unless-stopped
    expose:
      - "80"

  music-review-update:
    profiles:
      - jobs
    build:
      context: .
    env_file:
      - .env
    environment:
      MUSIC_REVIEW_PROJECT_ROOT: /app
      ARTIST_IMAGE_DOWNLOAD: "true"
    volumes:
      - ./data:/app/data
    command: ["python", "-m", "music_review.pipeline.production_update", "-v"]

  music-review-artist-images:
    profiles:
      - jobs
    build:
      context: .
    env_file:
      - .env
    environment:
      MUSIC_REVIEW_PROJECT_ROOT: /app
      ARTIST_IMAGE_DOWNLOAD: "true"
      ARTIST_IMAGE_RESOLVE_ON_DEMAND: "true"
      USER_AGENT_APP: Plattenradar
      USER_AGENT_VERSION: "1.0"
      USER_AGENT_CONTACT: https://github.com/tomasito12/music-review
    volumes:
      - ./data:/app/data
    command:
      [
        "python",
        "-m",
        "music_review.pipeline.enrichment.artist_image_batch_cli",
        "--missing-only",
        "--all",
        "--download",
        "-v",
      ]

  music-review-metadata-refresh:
    profiles:
      - jobs
    build:
      context: .
    env_file:
      - .env
    environment:
      MUSIC_REVIEW_PROJECT_ROOT: /app
      USER_AGENT_APP: Plattenradar
      USER_AGENT_VERSION: "1.0"
      USER_AGENT_CONTACT: https://github.com/tomasito12/music-review
    volumes:
      - ./data:/app/data
    command:
      [
        "python",
        "-m",
        "music_review.pipeline.enrichment.fetch_metadata",
        "--update",
      ]

  music-review-update-db:
    profiles:
      - jobs
    build:
      context: .
    env_file:
      - .env
    environment:
      MUSIC_REVIEW_PROJECT_ROOT: /app
      USER_AGENT_APP: Plattenradar
      USER_AGENT_VERSION: "1.0"
      USER_AGENT_CONTACT: https://github.com/tomasito12/music-review
    volumes:
      - ./data:/app/data
    command: ["python", "scripts/update_database.py", "-v"]

volumes:
  caddy_data:
  caddy_config:
